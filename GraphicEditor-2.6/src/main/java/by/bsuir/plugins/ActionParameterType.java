package by.bsuir.plugins;

// enumeration of actions on the figure (change color, scale, mirror reflection, delete)
public enum ActionParameterType {
    COLOR_CHANGE,
    SIZE_CHANGE,
    MIRROR_REFLECTION,
    REMOVE
}
